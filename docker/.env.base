###
# Runtime settings
###

# Isaac Lab image to run directly (no local Docker build required).
ISAACLAB_IMAGE=nvcr.io/nvidia/isaac-lab:2.3.2
CONTAINER_NAME=legged-lab

# Path is resolved relative to docker/docker-compose.yaml.
RSL_RL_PATH=../../rsl_rl

# Store all Isaac Sim caches/logs under the repository's docker directory.
# Path is resolved relative to docker/docker-compose.yaml.
DOCKER_CACHE_ROOT=./.cache

# Default network bypass list for proxy propagation.
NO_PROXY=localhost,127.0.0.1
